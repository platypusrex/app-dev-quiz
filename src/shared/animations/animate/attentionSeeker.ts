import {
  style,
  state,
  transition,
  animate,
  keyframes,
} from '@angular/animations';
import { AnimationMetadata } from '@angular/core';

export const attentionSeeker = (timing:string) => [

];